<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Abs.DBCC.Desktop.ViewModels"
             xmlns:collation="using:Abs.DBCC.Domain.Collation"
             xmlns:res="using:Abs.DBCC.Desktop.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="550"
             x:Class="Abs.DBCC.Desktop.Views.TargetCollationPickerView"
             x:DataType="vm:TargetCollationPickerViewModel">

    <DockPanel Margin="24">

        <StackPanel DockPanel.Dock="Top" Spacing="8" Margin="0,0,0,12">
            <TextBlock Text="{x:Static res:Strings.SelectTargetCollationTitle}" FontSize="20" FontWeight="Bold" />
            <TextBox Text="{Binding SearchText}" PlaceholderText="{x:Static res:Strings.SearchPlaceholder}" />
            <TextBlock Text="{x:Static res:Strings.Loading}" IsVisible="{Binding IsLoading}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Spacing="8" Margin="0,12,0,0">
            <Button Content="{x:Static res:Strings.BackButton}" Command="{Binding BackCommand}" />
            <CheckBox Content="{x:Static res:Strings.UpdateDatabaseDefaultCollationLabel}" IsChecked="{Binding UpdateDatabaseDefaultCollation}" VerticalAlignment="Center" />
            <Button Content="{x:Static res:Strings.BuildPlanButton}" Command="{Binding BuildPlanCommand}" IsEnabled="{Binding !IsBuildingPlan}" />
            <TextBlock Text="{x:Static res:Strings.BuildingPlan}" IsVisible="{Binding IsBuildingPlan}" VerticalAlignment="Center" />
        </StackPanel>

        <ScrollViewer>
            <ListBox ItemsSource="{Binding FilteredCollations}" SelectedItem="{Binding SelectedCollation}">
                <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="collation:CollationInfo">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                            <TextBlock Text="{Binding Description}" FontSize="11" Opacity="0.7" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

    </DockPanel>

</UserControl>
